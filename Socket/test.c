#include <stdio.h>
#include <string.h>

int main()
{
    char msg[100];
    scanf("%s", &msg);
    printf("%s", msg);
    // for (int i = 0; i < strlen(msg); i++)
    // {
    //     printf("%c",msg[i]);
    // }
    return 0;
}